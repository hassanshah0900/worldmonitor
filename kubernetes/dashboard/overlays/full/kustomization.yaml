apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -full

labels:
  - pairs:
      app.kubernetes.io/instance: full
    includeSelectors: true

images:
  - name: worldmonitor
    newName: syedhassanshah/worldmonitor
    newTag: pending-ci # {"$imagepolicy": "flux-system:worldmonitor-full:tag"}
