apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: worldmonitor-redis
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/redis
  prune: true
  sourceRef:
    kind: GitRepository
    name: worldmonitor
  dependsOn:
    - name: cluster-infra-secret-store
