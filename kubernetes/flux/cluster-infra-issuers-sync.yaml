apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-infra-issuers
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/cluster-infra/issuers
  prune: true
  sourceRef:
    kind: GitRepository
    name: worldmonitor
  dependsOn:
    - name: cluster-infra-helm
