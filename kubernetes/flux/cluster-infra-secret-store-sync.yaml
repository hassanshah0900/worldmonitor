apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-infra-secret-store
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/cluster-infra/secret-store
  prune: true
  sourceRef:
    kind: GitRepository
    name: worldmonitor
  dependsOn:
    - name: cluster-infra-helm
