apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -happy

labels:
  - pairs:
      app.kubernetes.io/instance: happy
    includeSelectors: true

images:
  - name: worldmonitor
    newName: syedhassanshah/worldmonitor-happy
    newTag: pending-ci # {"$imagepolicy": "flux-system:worldmonitor-happy:tag"}
