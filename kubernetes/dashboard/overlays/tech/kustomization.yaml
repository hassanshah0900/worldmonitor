apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -tech

labels:
  - pairs:
      app.kubernetes.io/instance: tech
    includeSelectors: true

images:
  - name: worldmonitor
    newName: syedhassanshah/worldmonitor-tech
    newTag: pending-ci # {"$imagepolicy": "flux-system:worldmonitor-tech:tag"}
