apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -finance

labels:
  - pairs:
      app.kubernetes.io/instance: finance
    includeSelectors: true

images:
  - name: worldmonitor
    newName: syedhassanshah/worldmonitor-finance
    newTag: pending-ci # {"$imagepolicy": "flux-system:worldmonitor-finance:tag"}
