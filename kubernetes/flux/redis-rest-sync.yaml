apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: worldmonitor-redis-rest
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/redis-rest
  prune: true
  sourceRef:
    kind: GitRepository
    name: worldmonitor
  dependsOn:
    - name: cluster-infra-secret-store
