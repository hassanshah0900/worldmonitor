apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: worldmonitor-infra-configs
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/infrastructure/configs
  prune: true
  sourceRef:
    kind: GitRepository
    name: worldmonitor
  dependsOn:
    - name: worldmonitor-infra-controllers
