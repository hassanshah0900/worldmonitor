apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -commodity

labels:
  - pairs:
      app.kubernetes.io/instance: commodity
    includeSelectors: true

images:
  - name: worldmonitor
    newName: syedhassanshah/worldmonitor-commodity
    newTag: pending-ci # {"$imagepolicy": "flux-system:worldmonitor-commodity:tag"}
